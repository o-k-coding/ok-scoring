import { featuresGameModels } from './features-game-models';

describe('featuresGameModels', () => {
  it('should work', () => {
    expect(featuresGameModels()).toEqual('features-game-models');
  });
});
