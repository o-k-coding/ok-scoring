import { featuresGameRulesFns } from './features-game-rules-fns';

describe('featuresGameRulesFns', () => {
  it('should work', () => {
    expect(featuresGameRulesFns()).toEqual('features-game-rules-fns');
  });
});
