import { featuresGameUiStore } from './features-game-ui-store';

describe('featuresGameUiStore', () => {
  it('should work', () => {
    expect(featuresGameUiStore()).toEqual('features-game-ui-store');
  });
});
